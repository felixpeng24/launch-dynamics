# launch-dynamics
Reusable launch vehicle landing dynamics simulation in Python (SciPy). Models 3-DOF propulsive descent (vertical, lateral, pitch) with nonlinear contact dynamics and throttle-down thrust. Runs 5,000 Monte Carlo cases varying velocity, drift, pitch, and thrust uncertainty; computes interface loads and statistics.
